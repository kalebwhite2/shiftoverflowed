{
    "scripts": {
        "server": "nodemon ./server/server",
        "client": "npm start --prefix ./client/",
        "dev-local": "concurrently -k --prefix-colors blue,white -n server,client \"npm:server\" \"npm:client\""
    },
    "dependencies": {
        "concurrently": "^8.2.2",
        "nodemon": "^3.0.3"
    }
}
