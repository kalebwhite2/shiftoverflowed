<%= form_with model: @user, url: reset_password_path do |form| %>
    <%= form.label :email, "Email: " %>
    <%= form.text_field :email, class: "form-control mb-3" %>

    <%= form.submit "Reset Password", class:"btn btn-primary" %>
<% end %>