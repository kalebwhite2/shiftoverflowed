<nav class="navbar navbar-expand-lg navbar-secondary bg-secondary sticky-top">
    <div class="container-fluid">
        <div class="fs-1 text-primary">shiftoverflowed</div>
        <!-- Check the session to see if a user is logged in -->
        <% if Current.user %>
            <div class="d-flex">
                <%= link_to 'Full Schedule', '/user/calendar', class:"btn btn-outline-primary m-1" %>
                <%= link_to 'Update Availability', '/user/calendar', class:"btn btn-outline-light m-1" %>
                <%= link_to 'User Profile', '/user/profile', class:"btn btn-outline-light m-1" %>
                <!-- We use button_to here because it generates a hidden form that specifies 
                http method as delete. -->
                <%= button_to 'Log Out', logout_path, method: :delete, class:"btn btn-outline-warning m-1" %>
            </div>
        <% end %>
    </div>
</nav>